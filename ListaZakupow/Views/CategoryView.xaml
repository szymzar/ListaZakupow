<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ListaZakupow.Models"
             xmlns:views="clr-namespace:ListaZakupow.Views"
             x:Class="ListaZakupow.Views.CategoryView"
             x:DataType="models:Category">
    <Frame Margin="0,5">
        <VerticalStackLayout>
            <Grid ColumnDefinitions="*, Auto" Padding="10">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Tapped="ToggleExpansion_Tapped" />
                </Grid.GestureRecognizers>
                <Label Text="{Binding Name}" FontSize="22" FontAttributes="Bold" VerticalOptions="Center" />
                <Label Grid.Column="1" FontSize="22" VerticalOptions="Center">
                    <Label.Style>
                        <Style TargetType="Label">
                            <Setter Property="Text" Value="[+]" />
                            <Style.Triggers>
                                <DataTrigger TargetType="Label" Binding="{Binding IsExpanded}" Value="True">
                                    <Setter Property="Text" Value="[-]" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Label.Style>
                </Label>
            </Grid>

            <CollectionView ItemsSource="{Binding Products}"
                            IsVisible="{Binding IsExpanded}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Product">
                        <views:ProductView />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </Frame>
</ContentView>
